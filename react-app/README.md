# CareerCompass — React + Vite

```bash
npm install
npm run dev
```

Structure:
- `index.html`
- `src/main.jsx` — entry, routes
- `src/pages/` — Home, Compare, Book, NotFound
- `src/components/Layout.jsx` — Navbar, Footer, ChatBubble
- `src/styles.css` — theme
